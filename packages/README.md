# Shared packages

- `contracts/`: schemas shared across API, worker, and web client.
- `image-core/`: image decoding, orientation, crop validation, encoding, and report artifact logic.
- `runtime/`: environment settings, SQLite queue state, retention, and terminal-state guards.
