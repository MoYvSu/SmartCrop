# Applications

- `web/` will contain the browser client.
- `api/` will contain the HTTP API and job lifecycle service.

No framework has been scaffolded yet. Product and deployment decisions must be settled before choosing one.
