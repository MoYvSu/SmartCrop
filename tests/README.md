# Tests

- `contract/`: schema and compatibility tests;
- `integration/`: API, storage, worker, model-adapter, and artifact tests;
- `e2e/`: real browser workflows using representative fixture images.

GPU-dependent tests must be marked separately from CPU-safe tests.
